export function AnimatedStatistics({ stats }: { stats: { value: string; label: string }[] }) {
  return <section aria-labelledby="research-in-numbers" className="relative z-10 border-y border-border/60 bg-background/60 py-5 backdrop-blur-xl"><h2 id="research-in-numbers" className="sr-only">Research in numbers</h2><dl className="container grid grid-cols-2 gap-px overflow-hidden rounded-3xl border border-border/70 bg-border/70 lg:grid-cols-4">{stats.map((stat, index) => <div key={stat.label} className="reveal bg-background/90 px-5 py-7 sm:px-8" style={{ "--reveal-delay": `${index * .06}s` } as React.CSSProperties}><dd className="font-display text-4xl tracking-tight text-primary sm:text-5xl">{stat.value}</dd><dt className="mt-2 text-xs font-medium uppercase tracking-[.13em] text-muted-foreground">{stat.label}</dt></div>)}</dl></section>;
}
